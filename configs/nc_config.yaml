model: GCN
dataset: IMDB
tags:
  - GNN
  - baseline
  - node classification
defaults:
  - trainer: trainer
  - override hydra/launcher: submitit_slurm
hydra:
  launcher:
    account: COMPUTERLAB-SL2-GPU
    timeout_min: 60
    nodes: 1
    partition: ampere
    gres: "gpu:1"
    gpus_per_task: 1