# @package _global_
defaults:
  - override /data: amazon_books
  - override /model: gat

callbacks:
  model_checkpoint:
    filename: GAT-AmazonBooks

tags:
  - GAT
  - AmazonBooks
  - lp
  - gnn
  - exp2