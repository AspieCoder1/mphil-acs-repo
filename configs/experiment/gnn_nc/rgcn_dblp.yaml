# @package _global_
defaults:
  - override /dataset: dblp
  - override /model: rgcn

callbacks:
  model_checkpoint:
    filename: RGCN-DBLP

dataset:
  homogeneous: true

tags: [ RGCN, DBLP, exp1_1, node_classification, gnn ]