# @package _global_
defaults:
  - override /dataset: acm
  - override /model: rgcn

callbacks:
  model_checkpoint:
    filename: RGCN-ACM

dataset:
  homogeneous: true

tags: [ RGCN, ACM, exp1_1, node_classification, gnn ]
