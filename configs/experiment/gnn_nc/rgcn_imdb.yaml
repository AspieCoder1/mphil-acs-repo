# @package _global_
defaults:
  - override /dataset: imdb
  - override /model: rgcn

callbacks:
  model_checkpoint:
    filename: RGCN-IMDB

dataset:
  homogeneous: true

tags: [ RGCN, IMDB, exp1_1, node_classification, gnn ]